# Required server-side only, for api/chat.ts (the AI concierge).
# Never commit a real value here — this file documents the variable name only.
# Get a key at https://console.anthropic.com/, then set it in .env.local for local
# development (via `vercel dev`) and in your Vercel project's Environment Variables
# for production.
ANTHROPIC_API_KEY=
