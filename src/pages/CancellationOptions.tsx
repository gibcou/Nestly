import SimplePage from '../components/SimplePage';

export default function CancellationOptions() {
  return (
    <SimplePage
      title="Cancellation Options"
      subtitle="Flexible policies designed to work for both guests and hosts."
    >
      <p>
        Cancellation terms vary by listing and are shown before you book.
        In general, Nestly supports Flexible, Moderate, and Strict
        cancellation policies — look for the policy badge on each listing
        page for the specifics that apply to your reservation.
      </p>
    </SimplePage>
  );
}
